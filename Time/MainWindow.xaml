<Window x:Class="Time.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Time"
        mc:Ignorable="d"
        Title="TimeManagement" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid>
        <DataGrid x:Name="dgTodoList" FontSize="15" FontWeight="Bold" Margin="10,40,10,5"  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Path = FinishDate}" FontWeight="Bold" IsReadOnly="False" Header="FinishDate"  Width="180">
                </DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding Path = Text}" IsReadOnly="False" Header="Todo" Width="*"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <TextBlock x:Name="TxtBlochjara" Margin="10" FontWeight="Bold" FontSize="20"
               Text="Today" HorizontalAlignment="Left" VerticalAlignment="Top" />
        
    </Grid>
 
</Window>
